{
  "name": "chrome-tab-refresher",
  "version": "1.1.0",
  "private": true,
  "license": "MIT",
  "description": "Minimal Manifest V3 Chrome extension that auto-refreshes the active tab.",
  "scripts": {
    "hooks": "git config core.hooksPath .githooks",
    "icons": "node ../tools/make-icons.mjs",
    "pack": "bash ../tools/pack.sh",
    "test": "node ../tools/test.mjs && node test/worker.test.mjs"
  }
}
